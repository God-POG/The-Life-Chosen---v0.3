// data/state/migrations.js
// Intentional no-op scaffold until save migrations are needed.

window.GAME_STATE_MIGRATIONS = window.GAME_STATE_MIGRATIONS || {
  migrate(raw) {
    return raw;
  }
};
